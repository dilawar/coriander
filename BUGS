Known bugs for version 0.27.1:

- The REAL service is untested and only runs with some distros. It does not
  work on RH-7.2
- ISO has some problems with multi camera: some flows are mixed or don't show
  up correctly.
