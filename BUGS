Known bugs for version 0.99.3:

- full-screen should zoom the image to screen resolution
- someone experienced a problem with the BuildTriggerMenu
  function. I can't reproduce it, but it seems there's
  somthing lurking there. 